<h2><%= __("samples") %></h2> 

<div id="content">
    <div class="row">
        <div class="col-sm-12">
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th><%= __("arrival-code") %></th>
                            <th><%= __("biobank-code") %></th>
                            <th><%= __("type") %></th>
                            <th><%= __("donor") %></th>
                            <th><%= __("diagnosis") %></th>
                            <th><%= __("anatomical-position")%></th>
                            <!--
                            <th><%= __("birth-date") %></th>
                            <th><%= __("sex") %></th>
                            <th><%= __("project") %></th>
                            -->
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    <% _.each(samples, function(sample) { 
                        var metadata = sample.get("metadata") %> 
                    <tr>
                        <td><%= metadata["Arrival Code"].value[0] %></td>
                        <td><%= sample.get("biobankCode") %></td>
                        <td><%= sample.get("type").name %></td>
                        <td><%= sample.get("donor").code %></td>
                        <td><%= metadata.Diagnosis.value[0] %>
                        <td><%= metadata["Anatomical Position"] && metadata["Anatomical Position"].value[0] %></td>
                        <td><a class="btn" href="#/samples/edit/<%= sample.id %>"><%=__("edit") %></a></td>
                    </tr>
                    <% }) %> 
                    </tbody>
                </table>
            </div>
            <div id="buttonbardiv" class="row text-center">
                <a href="#/samples/new" class="btn btn-primary"><%= __("new-sample")%></a>
            </div>
        </div>
    </div>
</div>

